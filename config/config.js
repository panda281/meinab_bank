const config = {
  user: "root",
  password: "panda@123",
  host: "127.0.0.1",
  database: "meinab_bank",
  // waitForConnections: true,
  // connectionLimit: 10,
  // queueLimit: 0,
  port: 3306,
};

module.exports = config;
